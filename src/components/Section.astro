---
interface Props {
    title: string;
}

const { title } = Astro.props;
---

<section class="max-w-3xl mb-8 mx-auto">
    {title && <h2 class="text-2xl font-extrabold mb-4">{title}</h2>}
    <slot />
</section>
